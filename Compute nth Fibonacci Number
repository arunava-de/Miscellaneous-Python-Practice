def fib(n):

    # Compute the nth Fibonacci number
    fib = [0]*(n+1)
    
    if n==0:
        return 0
    fib[1] = 1
    
    for i in range(2,n+1):
        fib[i] = fib[i-1]+fib[i-2]
    
    return fib[n]

