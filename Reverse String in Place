def reverse(list_of_chars):

    # Reverse the input list of chars in place
    n = len(list_of_chars)
    halfway = n//2
    for i in range(halfway):
        list_of_chars[i], list_of_chars[n-i-1] = list_of_chars[n-i-1], list_of_chars[i]
    return list_of_chars
